*** Settings ***
Library    DatabaseLibrary

*** Variables ***
${BASE_URL}        API URL
${DB_HOST}         database host
${DB_USER}         username for connecting database
${DB_PASSWORD}     password for connecting database
${DB_NAME}         name of database 

${WEB_URL}         URL website
${USER_ID}         User id
${CART}            Database table of Cart